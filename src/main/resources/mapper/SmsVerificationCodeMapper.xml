<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pets_backend.dao.mapper.SmsVerificationCodeMapper">
    <select id="selectLatestValid" resultType="com.example.pets_backend.dao.entity.SmsVerificationCodeDO">
        SELECT id, phone, code, used, expires_at, created_at
        FROM sms_verification_codes
        WHERE phone = #{phone} AND used = 0 AND expires_at > NOW()
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <update id="markUsed">
        UPDATE sms_verification_codes
        SET used = 1
        WHERE id = #{id}
    </update>
</mapper>
